<nav id="main-nav" class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      <% if logged_in? %>
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
      <% end %>
      <%= link_to "DiVal Safety - First Aid", root_path, class: "navbar-brand" %>
    </div>

    <% if logged_in? %>
      <div class="collapse navbar-collapse">
        <ul class="nav navbar-nav navbar-right">
          <li class="right-20">
            <%= link_to account_pricing_path do %>
              <span class="glyphicon glyphicon-usd"></span>
              Check Pricing
            <% end %>
          </li>
          <% if current_user.admin? %>
            <li class="right-20">
              <%= link_to users_path do %>
              <span class="glyphicon glyphicon-user"></span>
              Techs
              <% end %>
            </li>
          <% end %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button">
              <%= current_user.first_name %> <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li>
                <%= link_to edit_user_path(current_user.id) do %>
                  <span class="glyphicon glyphicon-pencil right-5"></span>
                  <%= admin? ? 'Edit Profile' : 'Change Password' %>
                <% end %>
              </li>
              <li>
                <%= link_to logout_path, method: 'delete' do %>
                  <span class="glyphicon glyphicon-log-out right-5"></span>
                  Logout
                <% end %>
              </li>
            </ul>
          </li>
        </ul>
      </div>
    <% end %>
  </div>
</nav>
